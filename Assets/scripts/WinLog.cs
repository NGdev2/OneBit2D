using System;
using UnityEngine;
using UnityEngine.UI;

public class WinLog : MonoBehaviour
{
	// public Show_score script;
	public Text textObj;
	// private DateTime start_time;
    void Start()
    {
		// TODO: make correct time display
		// Debug.Log("past" + script.takeTime());
		// textObj.text = "Your playing time: " + (DateTime.Now.Ticks - script.takeTime()) / 1000;
    }

	
}
